# 根目录配置文件 - 仅用于开发
# 实际部署使用 docs/_config.yml

title: "SmartEVPedia (Development)"
description: "中国智能电动车软件与智驾演进百科 - 开发配置"
baseurl: ""  # 开发时使用空 baseurl
url: "http://localhost:4000"

# Build settings
markdown: kramdown
theme: minima
plugins:
  - jekyll-feed

# Exclude from processing
exclude:
  - docs/_site
  - vendor
  - Gemfile
  - Gemfile.lock
  - node_modules
