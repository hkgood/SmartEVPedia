title: "SmartEVPedia"
description: "中国智能电动车软件与智驾演进百科"
baseurl: "/SmartEVPedia"  # 对于用户页面，baseurl 应该为空
url: "https://hkgood.github.io"
github_username: "hkgood"

# Build settings
markdown: kramdown
theme: minima
plugins:
  - jekyll-feed

# Collections
collections:
  brands:
    output: true
    permalink: /brands/:path/

# Data files
data_dir: _data

# Include custom assets
include:
  - assets

# Sass configuration
sass:
  sass_dir: assets/css
  style: compressed

# Custom variables
brands:
  - lixiang

# Defaults
defaults:
  - scope:
      path: ""
      type: "brands"
    values:
      layout: "brand"
  - scope:
      path: ""
      type: "pages"
    values:
      layout: "default"

# Exclude from processing
exclude:
  - Gemfile
  - Gemfile.lock
  - node_modules
  - vendor/bundle/
  - vendor/cache/
  - vendor/gems/
  - vendor/ruby/
  - README.md